def division(poly,d):
    poly = [2,1,0,1]
    d = [1,1]
    #try:
  #      result = poly/d
   # except ZeroDivisionError:
  #      print("divide by zero")
    q = []
    r = poly
    while r != [] and poly[-1] >= r[-1]:
     #   t =
     #          q.setdefault(key, []).extend(value)
  #      for key, value in t.iteritems():
  #          r.setdefault(key,[]).extend(value)
  #      r =
    return (q, r)